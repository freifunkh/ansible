[NetDev]
Name={{ vxlan_iface_prefix }}-{{ '%02d' % domain.id | int }}
Kind=vxlan
MACAddress=88:e6:40:33:{{ '%x' | format(sn) }}0:{{ '%02d' % domain.id | int }}

[VXLAN]
Id={{ domain.vxlanid }}
# this changed on aiyions arch, so someday this will be correct in debian...
#VNI={{ domain.vxlanid }}
DestinationPort={{ vxlan_port }}
UDP6ZeroChecksumRx=true
UDP6ZeroChecksumTx=true
Local=fe80::1
