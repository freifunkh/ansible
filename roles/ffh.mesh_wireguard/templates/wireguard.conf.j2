[Match]
Name={{ mesh_wireguard_iface_prefix }}-{{ '%02d' % domain.id | int }}

[Network]
Address=fe80::1/64
# workaround for https://github.com/systemd/systemd/issues/23197
IPv6AcceptRA=false
VXLAN={{ vxlan_iface_prefix }}-{{ '%02d' % domain.id | int }}
