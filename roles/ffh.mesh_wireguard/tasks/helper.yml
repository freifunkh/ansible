---

- name: Create directory for wireguard helpers
  file:
    path: "{{ wg_helper_dir }}"
    state: directory

- name: Install vx.py
  template:
    dest: "{{ wg_helper_dir }}/vx.py"
    src: "vx.py.j2"
    mode: "u=rwx,g=rx,o=rx"

