domain (ip ip6) {
  table filter {
    chain INPUT_SOURCE_IS_NOT_FFH_EXIT {
      proto udp dport 10000:10100 ACCEPT;
    }
  }
  table nat {
    chain PREROUTING {
      proto udp dport 500 if {{ gre_lower_ifaces.default }} REDIRECT to-ports 10023;
    }
  }
}
